const prisma = require('../../../config/prisma')

const inactivate = ({ email, principalWord, linkContent }) => {
  const data = Object.keys(linkContent.data).map((key, index) => {
      if (Object)
  })
}

module.exports = inactivate
